{
  "name": "@plutus/constants",
  "version": "0.0.1",
  "main": "src/index.ts",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo node_modules",
    "format": "biome format .",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@plutus/tsconfig": "workspace:*",
    "date-fns": "4.1.0",
    "typescript": "^5.8.2"
  }
}
